# MoonVCR 需求规格

状态：阶段 2（需求与申报结构）已确认

项目名称：`MoonVCR：MoonBit HTTP 交互录制回放与离线契约测试库`

项目定位：MoonBit 的 HTTP 交互录制、回放与离线契约测试库。它把一次经过允许的真实请求保存为可审阅的 cassette，随后在开发和 CI 中不访问网络地重放，并在请求不匹配时给出结构化差异。

## 1. 背景与目标

许多 SDK 和服务端程序依赖第三方 HTTP API。现有测试往往需要真实凭据、稳定网络和可重复的服务状态，导致测试慢、昂贵、易受限流影响，也很难在无网络 CI 中复现。

MoonVCR 的目标是提供一套 MoonBit 优先、可审阅、默认保护敏感信息的录制/回放核心，让调用方以最小适配代码获得确定性的离线测试。当前对 Mooncakes 注册表的类别检索未发现直接同类的 `VCR/cassette/record-replay/http mock` 模块；这不是对全世界项目“不重复”的绝对保证，最终仍需在建仓和提交前再次核验。

## 2. 范围

### 2.1 MVP 必须包含

1. 类型化的请求、响应和交互记录模型。
2. 稳定 JSON cassette 格式：字段顺序、换行和数组顺序可确定，方便 Git review。
3. 可组合的请求匹配器：HTTP 方法、规范化 URL/查询参数、选定请求头、请求体摘要或内容。
4. 明确的 `record`、`replay` 和严格离线模式；回放未命中时返回类型化错误，不能暗中发起真实请求。
5. 默认敏感信息清理：至少覆盖 `Authorization`、`Cookie`、常见 API key 头/查询参数，并允许调用方声明 JSON 字段路径。
6. 结构化 mismatch 诊断：指出方法、URL、头、体或序号等差异，而不是只返回一条字符串错误。
7. transport-independent 适配接口，使 MoonBit HTTP 客户端可以接入而不要求透明代理。
8. 一个可直接运行的第三方 API 离线示例，以及无需网络即可运行的核心测试。

### 2.2 明确不做

- TLS MITM、系统级代理或自动拦截任意进程流量。
- 依赖外部服务才能通过的测试；真实 API 仅用于开发者主动录制示例 cassette。
- 在截止日前扩展成完整 API 测试平台、浏览器录制器、分布式 cassette 服务或复杂模板语言。
- 机械移植其他语言的 VCR 源码；实现、数据结构和 MoonBit API 由本项目重新设计。

## 3. 目标使用者与完整场景

### 场景 A：第三方 SDK 的单元测试

SDK 作者在一次有凭据的录制阶段访问地图、支付或 OCR 沙盒接口，提交脱敏后的 cassette。之后每次本地测试和 CI 都通过回放验证参数编码、错误处理和响应解析，不需要凭据或外网。

### 场景 B：受限流或不稳定供应商的回归测试

业务服务依赖有调用配额的供应商 API。开发者录制代表性成功、鉴权失败和限流响应，测试在 `replay` 模式固定复现这些分支，避免重复消耗配额，也避免供应商临时故障阻塞发布。

### 场景 C：内部 HTTP 服务的契约回归

服务团队将旧版本交互保存为 cassette。在接口升级后，回放同一请求并查看结构化差异，快速发现状态码、响应头或 JSON 响应结构变化；任何未登记的新请求在严格离线模式下直接失败。

### 场景 D：无网络教学与示例复现

开源项目把示例 cassette 和测试一起发布，贡献者只需安装 MoonBit 工具链即可运行完整示例，降低新贡献者的环境门槛。

## 4. 功能需求（EARS）

### FR-01 交互模型

系统应提供可序列化的 `Request`、`Response` 和 `Interaction` 类型，至少保存方法、URL、请求头、请求体、状态码、响应头和响应体；二进制或不可打印内容必须有明确编码策略。

### FR-02 确定性 cassette

当系统写入同一组交互时，系统应生成字节级稳定的 JSON 表示；重新读取后再写入不应产生无意义 diff。

### FR-03 规范化与匹配

当调用方配置了匹配策略时，系统应按方法、规范化 URL/查询参数、选定头和请求体策略逐项匹配；未配置的可变字段不能被隐式当作必须相等。

### FR-04 严格回放

当处于 `replay` 或严格离线模式且没有匹配交互时，系统必须返回包含候选记录和差异字段的类型化错误，并且不得调用底层网络传输。

### FR-05 录制

当处于 `record` 模式且底层适配器成功返回时，系统应保存请求和响应的完整核心信息，并在落盘前应用清理策略。

### FR-06 敏感信息清理

当 cassette 即将落盘时，系统应默认移除或替换 `Authorization`、`Cookie` 和常见 API key；调用方显式加入的 JSON 路径也必须被清理。清理结果应可在测试中断言。

### FR-07 差异诊断

当请求与现有 cassette 不匹配时，系统应至少区分方法、URL、查询参数、请求头、请求体和交互序号差异，并提供可读摘要。

### FR-08 适配边界

系统应定义小而稳定的 transport adapter 接口；核心匹配、存储、清理和诊断逻辑不得依赖某一个具体 HTTP 客户端。

### FR-09 示例与测试

系统应提供一个不需要网络的 `replay` 示例、一个明确标注需要主动录制的 `record` 示例，以及覆盖成功、未命中、清理和确定性序列化的核心测试。

## 5. 非功能需求

- 可复现：默认回放路径必须在无网络环境中运行。
- 安全：不把真实凭据写入示例、测试输出或仓库；README 明确录制前的凭据检查。
- 可维护：核心 API、cassette schema、错误类型和匹配规则有文档与测试。
- 可移植：优先使用 MoonBit 标准能力；平台相关代码限制在适配层，并记录已验证目标。
- 可审阅：每个 cassette 都是普通文本，变更可被 Git 逐行检查。
- 合规：采用 OSI 认可许可证；若借鉴成熟 VCR 概念而不复制代码，在 README 中明确“概念参考”和本项目原创实现边界。

## 6. 竞赛验收映射

| 赛事要求 | MoonVCR 交付证据 |
| --- | --- |
| MoonBit 为主要语言 | 核心模型、匹配、存储、清理、诊断和测试均以 MoonBit 实现 |
| GitHub 公共仓库与清晰历史 | 从初始化到发布保留真实、连续的功能提交；至少 10 个有内容的提交 |
| README 可复现 | 目标、安装、录制/回放、示例、无网络测试、限制和许可证说明 |
| CI | 自动执行格式检查、`moon check`、构建和测试 |
| 可运行示例 | 无网络 replay 示例；record 示例标注凭据和网络前置条件 |
| 核心路径测试 | codec、规范化、匹配、严格离线、清理、差异诊断和顺序回放 |
| 发布 Mooncakes | 完成包元数据、版本、README、许可证和发布前验证 |
| OSI 许可证 | 仓库根目录许可证文件及第三方/概念参考说明 |

## 7. 有意义的提交里程碑（不是拆分提交凑数）

提交应以可运行、可测试的独立功能为单位，预计至少包含以下 11 个里程碑：

1. 项目骨架、许可证、基础 CI 与最小可运行测试。
2. 请求/响应/交互模型与错误类型。
3. cassette schema、JSON 编解码和确定性输出测试。
4. URL、查询参数和头的规范化工具。
5. 可组合请求匹配器与候选选择规则。
6. record/replay 引擎与严格离线拒绝网络逻辑。
7. 默认敏感信息清理及自定义字段路径。
8. 结构化 mismatch 差异报告。
9. HTTP transport adapter 与最小端到端示例。
10. 核心测试矩阵、脱敏 fixture 和无网络 CI 验证。
11. README、Mooncakes 元数据、发布检查和最终示例完善。

每个里程碑都必须改变源代码、测试、文档或可验证配置，并在提交信息中说明用户可见结果；禁止空提交、重复提交或事后改写历史。

## 8. 报名申报书八项结构（内部事实提纲）

1. **项目名称**：`MoonVCR`；副标题可写“MoonBit HTTP 录制回放与离线契约测试库”。
2. **项目简介**：说明第三方 HTTP 依赖使测试不稳定，MoonVCR 用脱敏 cassette 和严格离线回放提供可重复测试。
3. **项目方向、通用性**：MoonBit 开发者工具 / Web 测试基础设施；适用于 SDK、服务端、内部 API 和教学示例。
4. **至少三个场景**：使用场景 A、B、C（可补充 D）。
5. **核心功能**：对应 FR-01 至 FR-09，报名版只保留最能体现价值的条目。
6. **原创/移植/参考**：原创 MoonBit 实现，不移植其他项目源代码；可在 README 透明说明仅参考 VCR 类工具的通用思想，并列出本项目新增的 MoonBit-first 设计。
7. **来源与许可证**：因非移植项目，写“无直接移植来源”；对概念参考、第三方依赖和其许可证逐项核查后再填，不能留未经核实的链接。
8. **GitHub 链接与提交数**：建仓后填真实 URL；提交达到至少 10 个有意义的功能里程碑，提交历史保持可审计。

报名页要求申报书控制在一页内。赛事工作人员已确认可以使用 AI 起草，因此后续可以由本项目协助完成申报书；提交前仍必须由参赛者核对仓库链接、功能状态、许可证和提交历史等事实，避免申报内容与 GitHub 实际不一致。

## 9. 阶段安排

1. 需求确认：确定名称、范围、许可证和验收边界。
2. 设计阶段：产出模块边界、cassette schema、适配器 API 和测试策略；经用户确认后才实现。
3. 实现阶段：按第 7 节的真实里程碑推进，每个阶段先汇报计划。
4. 验证与发布：本地无网络测试、CI、README、Mooncakes 发布检查、GitHub 公共仓库核验。
5. 报名材料：参赛者重写并确认一页申报书，填入最终项目名和真实仓库链接，再执行提交。

项目名称已确认。下一步进入“设计文档与任务拆解”阶段；设计和任务确认后再开始代码实现，优先形成可被赛事人员检查的仓库内容、测试、示例和 README。

## 10. 初审整改：响应契约与可靠性基线

本阶段只补齐已经获批的响应契约校验与可靠性测试，不修改报名申报书、不发布新版本，也不引入真实网络依赖。

### 用户故事

1. 作为 MoonBit SDK 维护者，我希望离线回放得到的响应能够校验状态码、必要响应头、body 类型和 JSON 字段类型，从而发现上游 API 的不兼容变化。
2. 作为 CI 维护者，我希望明确证明 replay 和 strict-offline 在命中、未命中及错误路径均不会调用 transport。
3. 作为开源项目审查者，我希望测试结果可重复、诊断不泄露正文或凭据，并能从测试名直接定位验收项。

### EARS 验收条件

- 当调用方校验响应时，MoonVCR 应按固定顺序检查允许的状态码、必要响应头、期望 body 类型和 JSON 字段规则。
- 当响应违反一项或多项契约时，MoonVCR 应返回结构化违约报告，且报告不得包含响应头值、JSON 字段值或完整 body 原文。
- 当 JSON body 无效、字段缺失或类型不符时，MoonVCR 应返回可定位到规则路径的类型化违约，而不是抛出未处理异常。
- 当 replay 或 strict-offline 命中、未命中或请求清理失败时，MoonVCR 均不得调用调用方提供的 transport。
- 当同一 cassette 和请求重复执行编码、解码、匹配或诊断时，MoonVCR 应产生相同结果。
- 当 record、编码、解码和 replay 组成完整闭环时，回放应返回已录制响应，且 replay 阶段 transport 调用次数应为零。

### 非目标

- 本阶段不实现 OpenAPI、JSON Schema 或完整 JSONPath。
- 本阶段不增加真实 HTTP 客户端、TLS MITM、系统代理或自动流量拦截。
- 本阶段不实现文件 store；cassette 持久化继续由调用方通过 `Cassette::encode` 和 `Cassette::decode` 接入宿主存储。

## 11. 阶段2发布与申报一致性

本阶段把初审整改结果同步到公开文档、包元数据和本地申报材料，但仍不在未完成最终复核前推送或发布。

### 用户故事

1. 作为包使用者，我希望安装命令、README、`moon.mod` 和申报材料使用同一个版本号，避免按文档安装到没有响应契约 API 的旧版本。
2. 作为赛事评审者，我希望只执行仓库中的命令就能复现 59 个测试、离线 demo 和可靠性矩阵，不需要真实网络或私人材料。
3. 作为参赛者，我希望含联系方式的申报书不会进入 GitHub 提交或 Mooncakes 发布归档。

### EARS 验收条件

- 当项目准备发布整改版时，`moon.mod`、README、发布清单和申报书应统一声明 `0.2.0`，且不得把尚未发布的版本写成已发布事实。
- 当维护者执行 `moon package --list` 时，归档应包含运行代码、测试、README、许可证、CI、demo、规格和可靠性说明，不应包含 `MoonVCR_submission.md`。
- 当评审者按 README 执行验收命令时，测试总数应为 59，离线 demo 应输出 `offline replay status=200` 和 `recorded interactions=1`。
- 当维护者检查报名材料时，申报书的八项内容、可靠性数字和不宣称边界应与仓库实际实现一致。

### 非目标

- 本阶段不代替参赛者向赛事表单提交材料；
- 本阶段不修改既有 Git 提交历史，不制造空提交或重复提交；
- 本阶段不宣称 0.2.0 已经完成 Mooncakes 正式发布，发布必须在独立的最终阶段完成并复验。
