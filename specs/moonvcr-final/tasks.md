# MoonVCR 终审完善实施任务

## A. 核心可靠性

- [x] A1. 为 header/query/body 结构冲突写回归测试，替换无歧义匹配键。
  - 验收：分隔符、重复项、空值和 Unicode 的正反例通过。
  - 需求：R1
- [x] A2. 从公开 `ReplayMiss` 移除完整匹配 key，统一安全错误摘要。
  - 验收：植入秘密在错误对象和 summary 中均不存在。
  - 需求：R2
- [x] A3. 补 URL 编码敏感参数、JSON 数组路径、请求/响应不同脱敏规则测试。
  - 验收：必需规则失败不写入 cassette；可选规则行为有文档。
  - 需求：R2
- [x] A4. 隔离 Session 输入快照，增加消费计数和完成检查。
  - 验收：少调用、多调用和多会话状态独立。
  - 需求：R3
- [x] A5. 修复 demo 和检查入口的失败退出码。
  - 验收：故意破坏 cassette 或契约时命令失败。
  - 需求：R7、R8

## B. 平台与实际使用

- [x] B1. 建立 HTTP/文件 IO 工具链和后端探针，记录版本和支持矩阵。
  - 验收：探针在声明支持的平台通过，未支持平台明确失败。
  - 需求：R4
- [ ] B2. 实现真实 loopback HTTP transport 适配和请求/响应转换（适配代码已完成，loopback 运行测试待 Ubuntu CI）。
  - 验收：GET、POST、header、JSON、字节 body、连接错误和超时有测试。
  - 需求：R4、R7
- [x] B3. 实现 cassette 文件加载、保存、大小限制和安全覆盖规则。
  - 验收：跨进程保存/加载成功，损坏或保存失败不破坏旧文件。
  - 需求：R5
- [x] B4. 运行文件录制→新进程离线回放集成流程；真实 HTTP 服务闭环仍待 CI/后续场景。
  - 验收：离线 transport 调用为零，业务断言和契约均通过。
  - 需求：R7

## C. 契约和场景

- [x] C1. 增加媒体类型/必要头值、可选字段和数组元素契约。
  - 验收：必要 header 值、可选字段缺席/错误类型、数组元素正确/错误类型均有结构化报告；不复制业务值。
  - 需求：R6
- [ ] C2. 完成分页 SDK、订单兼容、受限网络 CI 三个可运行示例（当前已交付核心/native 场景，业务模板待补）。
  - 验收：每个示例有成功和故障命令及输出。
  - 需求：R7、R9

## D. 工程交付

- [x] D1. 补异常、容量、格式和平台集成矩阵，移除 CI 重复步骤。
  - 验收：故意失败能使 CI 红灯；记录实际性能而不虚构承诺。
  - 需求：R8
- [x] D2. 更新 README、变更日志、贡献说明、API 和限制；0.3.0 独立消费者迁移验证待发布后。
  - 验收：干净消费者按 README 完成安装和示例。
  - 需求：R9
- [x] D3. 审核依赖 manifest、包清单和个人信息排除；正式发布前仍需保存最终清单证据。
  - 验收：发布包不含报名材料或凭据，许可来源可追踪。
  - 需求：R9

## E. 发布预演

- [ ] E1. 独立消费者安装拟发布版本，运行三个场景和失败用例。
  - 验收：源码与正式 Mooncakes 包行为一致。
  - 需求：R10
- [ ] E2. 冻结版本、标签、CI 和发布证据，更新验收矩阵。
  - 验收：材料只描述已经实测的能力，Git 历史不改写。
  - 需求：R10
