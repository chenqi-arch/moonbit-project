name = "chenqi-arch/moonbit-project"

version = "0.1.0"

readme = "README.md"

repository = "https://github.com/chenqi-arch/moonbit-project"

license = "Apache-2.0"

keywords = [ "http", "testing", "record-replay", "offline" ]

preferred_target = "wasm"

description = "MoonBit HTTP interaction recording, replay, and offline contract testing library"
